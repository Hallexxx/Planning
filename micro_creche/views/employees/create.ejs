<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ajouter un Employé</title>
  <!-- Ajouter la police Poppins et Bootstrap -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #f7f7f7;
      padding-top: 50px;
    }
    h2 {
      text-align: center;
      font-size: 2.5rem;
      font-weight: 600;
      color: #333;
      margin-bottom: 30px;
    }
    .form-container {
      max-width: 800px;
      margin: 0 auto;
      background-color: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease-in-out;
    }
    .form-container:hover {
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
    }
    .form-label {
      font-weight: 600;
      color: #555;
    }
    .btn-primary {
      background-color: #007bff;
      border: none;
      padding: 12px 20px;
      font-size: 1.2rem;
      font-weight: 600;
      border-radius: 5px;
      width: 100%;
    }
    .btn-primary:hover {
      background-color: #0056b3;
      cursor: pointer;
    }
  </style>
</head>
<body>

<div class="container">
  <h2>Ajouter un Employé</h2>
  <div class="form-container">
    <form action="/employee/add" method="POST">
      <div class="mb-3">
        <label for="name" class="form-label">Nom de l'employé</label>
        <input type="text" class="form-control" id="name" name="name" required>
      </div>
      <div class="mb-3">
        <label for="email" class="form-label">Email</label>
        <input type="email" class="form-control" id="email" name="email" required>
      </div>
      <div class="mb-3">
        <label for="phone" class="form-label">Numéro de téléphone</label>
        <input type="tel" class="form-control" id="phone" name="phone">
      </div>
      <div class="mb-3">
        <label for="role" class="form-label">Rôle</label>
        <input type="text" class="form-control" id="role" name="role" value="employee" readonly>
      </div>
      <div class="mb-3">
        <label for="microCrecheId" class="form-label">Micro-crèche</label>
        <select class="form-select" id="microCrecheId" name="microCrecheId" required>
          <% microCreches.forEach(mc => { %>
            <option value="<%= mc._id %>"><%= mc.name %></option>
          <% }) %>
        </select>
      </div>
      <div class="mb-3">
        <label for="workHoursPerWeek" class="form-label">Nombre d'heures de travail par semaine</label>
        <input type="number" class="form-control" id="workHoursPerWeek" name="workHoursPerWeek" min="0" required>
      </div>

      <button type="submit" class="btn btn-primary">Ajouter l'employé</button>
    </form>
  </div>
</div>

<!-- Ajouter Bootstrap JS et jQuery pour les animations et interactions -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
