<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Micro-Crèches</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f8f9fa;
        }
        .card-bg {
            background-color: #e9ecef;
            transition: transform 0.3s;
        }
        .card-bg:hover {
            transform: scale(1.05);
        }
        .divider {
            border-top: 1px solid #dee2e6;
            margin: 20px 0;
        }
        .btn-custom {
            background-color: #5cb85c;
            color: white;
        }
        .btn-custom:hover {
            background-color: #4cae4c;
        }
    </style>
</head>
<body>

    <div class="container mt-5">
        <h2 class="text-center">Toutes vos Micro-Crèches</h2>
        <p class="text-center">Consultez toutes les micro-crèches que vous avez créées. Vous pouvez accéder à chaque micro-crèche pour voir plus de détails.</p>
        
        <div class="row">
            <% if (microCreches.length > 0) { %>
                <% microCreches.forEach((microCreche) => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card card-bg">
                            <div class="card-body">
                                <h5 class="card-title"><%= microCreche.name %></h5>
                                <p><%= microCreche.description || "Aucune description pour cette micro-crèche." %></p>
                                <a href="/micro-creche/<%= microCreche._id %>" class="btn btn-primary">Voir Détails</a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-md-12">
                    <p>Vous n'avez pas encore de micro-crèche. <a href="/micro-creche/create" class="btn btn-primary">Créez-en une maintenant</a></p>
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
